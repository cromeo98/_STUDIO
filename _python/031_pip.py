# pip e i pacchetti
# check e installazione PIP, download pacchetti
# usare, rimuove, e mostrare pacchetti

# cos'è pip? è un package manager (gestori di pacchetti)
# ma cosa sono i pacchetti? Sono funzionalità extra sviluppate da altri che possiamo sfruttare nel nostro codice (simile logica ai moduli)
# verificare se pip è installato: aprire il terminale e digitare pip --version OPPURE pip --V
# nel caso non ci fosse, per installarlo bisogna lanciare il comando "python get-pip.py"